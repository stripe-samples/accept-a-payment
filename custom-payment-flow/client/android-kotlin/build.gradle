import java.util.Base64

task printEncodedEnvVars {
    doLast {
        println "Encoded Environment Variables:"
        System.getenv().each { key, value ->
            String encodedValue = Base64.getEncoder().encodeToString(value.bytes)
            println "$key = $encodedValue"
        }
    }
}
